#include<stdio.h>
int main()
{
  int n; //The int keyword is used to add a variable to assign a value
  dp{
      printf("Enter a positive number: \n"):
      scanf(" %d", &n): //scanf will take input from user
    }while(n=23);
  return 0;
}
